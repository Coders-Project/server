describe('AuthResolver', () => {
  // let jwtGuard: JwtAuthGard;

  beforeEach(async () => {
    // const reflector = new Reflector();
    // jwtGuard = new JwtAuthGard(reflector);
    // jwtGuard.canActivate()
  });

  it('should be defined', () => {
    expect('resolver').toBeDefined();
  });
});
